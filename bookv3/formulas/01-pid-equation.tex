% PID equation — Chapter 4 (מושגים)
% Insert at marker: [נוסחה 01 — משוואת PID]
% Render in CodeCogs (link in formulas/README.md) or compile locally with pdflatex.

\documentclass[border=8pt]{standalone}
\usepackage{amsmath}
\begin{document}
$$ u(t) = K_p \cdot e(t) + K_i \int e(t)\, dt + K_d \cdot \frac{de}{dt} $$
\end{document}
